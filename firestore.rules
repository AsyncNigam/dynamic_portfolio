rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down everything by default
    match /{document=**} {
      allow read: if true; // Public read access for portfolio
      allow write: if false; // Private, environment-locked write blockades (Admin SDK bypasses this)
    }
  }
}
